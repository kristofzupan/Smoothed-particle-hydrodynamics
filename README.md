# Smoothed Particle Hydrodynamics

2D SPH fluid simulation in Python.

## Install

```
pip install -r requirements.txt
```

## Run

CPU version:
```
python main.py
```

GPU version (macOS only — uses Metal via Taichi):
```
python sph_gpu.py
```